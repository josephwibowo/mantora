version: "3.9"

services:
  postgres:
    image: postgres:16
    environment:
      POSTGRES_USER: mantora
      POSTGRES_PASSWORD: mantora
      POSTGRES_DB: mantora_demo
    ports:
      - "5432:5432"
    volumes:
      - ./init.sql:/docker-entrypoint-initdb.d/init.sql:ro
